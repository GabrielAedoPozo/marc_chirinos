<main class="hero-section">
  <div class="hero-content">
    <h1>¡Bienvenido!</h1>
    <p>Esta es tu página principal después de la animación épica.</p>
    <p>Puedes seguir agregando más contenido aquí.</p>
  </div>
</main>

<style>
  .hero-section {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(to bottom, #f8f9fa, #e9ecef);
    padding: 2rem;
    position: relative;
  }

  .hero-content {
    text-align: center;
    max-width: 800px;
  }

  .hero-content h1 {
    font-size: clamp(2rem, 5vw, 4rem);
    color: #212529;
    margin-bottom: 1.5rem;
    font-weight: 700;
  }

  .hero-content p {
    font-size: clamp(1rem, 2vw, 1.5rem);
    color: #495057;
    margin: 0.5rem 0;
  }
</style>